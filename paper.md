---
title: "An open-source framework for FAIR climate-ecotoxicology research: integrating Copernicus Marine data with biological sentinel monitoring in the Mediterranean Sea"
tags:
  - Python
  - Open Science
  - FAIR data
  - marine heatwaves
  - ecotoxicology
  - reproducibility
  - Streamlit
  - Copernicus Marine
authors:
  - name: Guido Scatena
    orcid: 0000-0002-1641-5433
    affiliation: 1
  - name: Andrea Gaion
    orcid: 0000-0002-5694-4049
    affiliation: 1
  - name: Davide Sartori
    orcid: 0000-0001-9197-3083
    affiliation: 1
affiliations:
  - name: ISPRA – Italian Institute for Environmental Protection and Research
    index: 1
date: 2026-03-31
bibliography: paper.bib
---

# Summary

We present an end-to-end open-source framework that demonstrates how Open Science principles
(Open Data, Open Source, Open Methodology, and end-to-end reproducibility) can be concretely
implemented in marine ecotoxicological research. The primary contribution is methodological: the
framework shows how to bridge the data silos that separate large-scale physical oceanography from
local biological monitoring, building a fully transparent and reproducible analytical pipeline
over heterogeneous sources.

As a proof of concept, the framework is applied to a 23-year dataset (2003--2025) on the gamete
toxicity of *Paracentrotus lividus* (Mediterranean purple sea urchin) in the Ligurian Sea (off
Livorno, 43.43°N 10.40°E), combining five Copernicus Marine environmental variables with 158
months of real copper embryotoxicity bioassay data (EC$_{50}$). All data, source code, and
pre-computed results are public, permanently archived on Zenodo [@scatena_2026], and deployed on
Streamlit Community Cloud, embodying FAIR principles (Findable, Accessible, Interoperable,
Reusable) at every layer.

# Statement of Need

Physical oceanographic observations (available at scale through services such as Copernicus
Marine) and ecotoxicological bioassay records (generated by environmental agencies) habitually
reside in isolated silos. This fragmentation limits the ability of researchers and policy-makers
to detect and model climate-driven biological change in a reproducible, auditable way.

Standalone tools address parts of this problem: `marineHeatWaves.py` [@hobday_2016] and
`heatwaveR` [@schlegel_2018] detect Marine Heatwave (MHW) events; dedicated packages exist for
dose-response and distributed lag modelling [@gasparrini_2010]. No open framework connects these
resources into a single pipeline, from raw data ingestion to public interactive interface,
while exposing every intermediate step for independent verification.

`climate_change_on_sea_urchins` fills this gap. It is not primarily a tool for sea urchins: it
is an open, auditable, and directly citable template for integrating heterogeneous environmental
data streams according to FAIR principles. Site and data-source identifiers are isolated in a
single configuration module, and a dedicated guide documents what is required to point the
pipeline at a different site, species, or biological indicator. The target audience includes
marine ecotoxicologists, environmental monitoring agencies, and any research group connecting
Earth observation data with biological sentinel measurements. The approach has already
supported a peer-reviewed longitudinal study on metal sensitivity trends in *P. lividus*
[@sartori_2023], empirically validating its scientific utility.

# Functionality

The framework is organised as a linear, four-stage pipeline in which each stage produces
versioned, file-based artefacts consumed by the next, with no hidden state or tight coupling
between components (Figure 1). Any stage can therefore be re-run, replaced, or audited
independently: a researcher who distrusts one analysis script can re-execute just that script
and inspect its CSV output without touching the rest of the pipeline.

![Four-stage reproducible pipeline. Three inputs feed `scripts/`: Copernicus Marine (monthly
means and daily SST), ISPRA Bioassays (monthly EC$_{50}$), and `marineHeatWaves.py`
[@hobday_2016] (vendored MHW detection algorithm). The `climate_change_on_sea_urchins` Python
package runs the statistical pipeline and writes artefacts to `results/`, consumed
independently by the dashboard and the permanent archive (Zenodo, GitHub).](figures/fig0_pipeline.png)

## Data Pipeline

Three inputs feed into `scripts/`. The `copernicusmarine` Python client queries
MEDSEA_MULTIYEAR products to extract five oceanographic variables (SST, salinity, dissolved
oxygen, pH, surface CO$_2$) as monthly means at the monitoring site, plus daily SST for MHW
detection. Monthly EC$_{50}$ bioassay records are fetched live from a cloud spreadsheet maintained by
ISPRA (the Italian Institute for Environmental Protection and Research), the national agency
running the underlying ecotoxicological monitoring programme. MHW events are detected by
`scripts/detect_mhw.py` using `marineHeatWaves.py` [@hobday_2016], a community-standard
algorithm vendored under its original licence, applying a 90th-percentile threshold over at
least five consecutive days. `scripts/build_dataset.py` aligns all three streams on a common
monthly DatetimeIndex, producing `data_extended.csv` (276 months, 2003--2025) and
`data_ec50_ci.csv` (EC$_{50}$ values with 95% CI and an `EC50_imputed` flag). All downstream analyses use only the months with real EC$_{50}$ measurements (158 at the time
of writing) and the MHW catalogue (129 events, four intensity categories); both grow
automatically as new bioassays are submitted, so the same pipeline that supports historical
analysis also functions as a live monitoring instrument. Figure 2 illustrates the co-evolution
of EC$_{50}$ toxicity and MHW activity over the study period.

![EC$_{50}$ time series (top) and annual MHW days (bottom) at the Livorno monitoring site,
2003--2025. Shaded periods indicate individual MHW events; the dashed vertical line marks the
2016 split, identified by @sartori_2023 as the discriminant year for the change in gamete
sensitivity, and used as the default (but interactively adjustable) split point throughout the
period-comparison analyses.](figures/fig1_timeseries.png)

## Analysis Modules

`climate_change_on_sea_urchins` is installed with `pip install -e .` (a standard
`src`-layout package, `pyproject.toml`-configured) and exposes a console entry point,
`ccsu-run-pipeline`, that orchestrates six modules implementing the full statistical
pipeline and writing structured outputs to `results/` as CSV and JSON; every intermediate
result, and every module, is independently importable, inspectable, and reusable
(`from climate_change_on_sea_urchins import load_data`, etc.):

- **`timeseries`**: STL seasonal decomposition; PCA anomaly detection.
- **`period_split`**: Kruskal--Wallis and Mann--Whitney *U* tests pre/post a split year
  (2016 by default, per @sartori_2023); rank-biserial effect sizes.
- **`correlations`**: Spearman correlation matrices (full period, pre-2016, post-2016),
  including MHW metrics.
- **`stationarity`**: ADF and KPSS tests; automatic first-differencing for
  non-stationary variables.
- **`mhw_analysis`**: Spearman cross-correlation function (CCF) at lags 0--12 months
  (peak at lag 2: r = -0.373, p = 1.4 × 10^{-6}, consistent with the gametogenesis cycle of
  *P. lividus*); Granger causality F-tests; ARDL(3,12) cumulative response model.
- **`forecast`**: SARIMAX(1,0,1)(1,0,1,12) three-scenario forecast to 2040.

A complementary R script (`scripts/mhw_lag_analysis.R`) adds superposed epoch analysis, DLNM
[@gasparrini_2010], and mixed-effects models, with outputs saved as CSV for dashboard consumption.

## Dashboard and Reproducibility

The package's dashboard module exposes all pre-computed results through a nine-tab Streamlit
interface, launched via the `ccsu-dashboard` console entry point (equivalently,
`streamlit run app.py` from a repository checkout) and deployed at
<https://climate-change-on-sea-urchins.streamlit.app> (Figure 3): time-series visualisation
with CI bands and MHW overlays; event catalogue and annual trends; lag and causal analysis
(CCF, ARDL, Granger heatmap); pre/post comparisons with an adjustable split year; correlation
heatmaps per sub-period; stationarity summaries; three-scenario forecast; and a methods/about
panel. The interface fetches EC$_{50}$ data live from the ISPRA source each session (with
static fallback) and provides a CSV download on every chart for downstream reuse. With one
exception, no analysis runs at dashboard runtime: computation and presentation are separate
modules of the same package, separated to guarantee reproducibility for the expensive models
(SARIMAX, ARDL, DLNM). The exception is the pre/post comparison, recomputed live because
non-parametric tests on a few hundred points are cheap, letting users test alternative split
years beyond the published 2016 discriminant without leaving the browser.

The full stack embodies FAIR principles: all code and data are distributed under an MIT licence
and archived with a persistent DOI on Zenodo
(DOI: [10.5281/zenodo.20745476](https://doi.org/10.5281/zenodo.20745476)) [@scatena_2026];
a `CITATION.cff` with author ORCIDs enables standard attribution; a Binder environment allows
browser-based execution without local setup; and a pytest suite (38 tests, including a
synthetic-data check that the analysis layer works on a structurally different dataset) with
GitHub Actions
CI verifies pipeline integrity on every commit. The framework is designed to be adapted: any
research group can replace the EC$_{50}$ bioassay source with another biological indicator and
re-run the full pipeline with minimal changes confined to the data ingestion layer.

By making rigorously validated environmental data, analysis workflows, and results freely
accessible, `climate_change_on_sea_urchins` is a virtuous example of how open science can
bridge expert environmental monitoring and the broader need for transparent, reproducible
climate information available to the scientific community and to society as a whole.

![Example screenshots from two of the dashboard's nine tabs; the full interface offers
substantially more analytical depth than these two views alone. (a) The Marine Heatwaves tab:
daily SST with MHW event shading, annual event counts and peak intensity trends, and the full
event catalogue with CSV export. (b) The MHW → Gametes (lag) tab: the causal analysis suite,
including annual MHW-vs-EC$_{50}$ trends and the EC$_{50}$ decline rate before and after 2016
(-1.21 mg/L/yr post-2016 vs +0.33 mg/L/yr pre-2016; acceleration factor 3.6×). Every chart has
a dedicated CSV download button.](figures/fig2_app_screenshot.png)

# Acknowledgements

Environmental data were obtained from the Copernicus Marine Service (MEDSEA_MULTIYEAR products).
MHW detection uses `marineHeatWaves.py` by Hobday et al. (2016), included under its original licence.

**AI usage disclosure**: Claude (Anthropic), using the latest model versions available at the
time of each development session (Claude Sonnet 4.6 at the time of this manuscript's
preparation), was used throughout this project's development for code generation and
refactoring (analysis pipeline modules, Streamlit dashboard features), debugging
(dependency-version compatibility fixes), test scaffolding, documentation (README,
CONTRIBUTING.md, CITATION.cff), and drafting/copy-editing of this manuscript. All AI-assisted
code and text were reviewed, tested, and validated by the human authors, who made all core
scientific and design decisions, verified factual claims (e.g., sampling site, author
attribution), and are fully responsible for the accuracy and originality of the final
submission.

# References
